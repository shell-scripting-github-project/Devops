git clone
export username
export token
helper function
ls -la
git status
git add <filename>
git log
git diff
git commit -m "YOUR MESSAGE"
git reset --hard <NUMBERS> (version control)
git init
git -lrta
git -a
git checkout <filename> (to disguard changes in file after add)
git remote -v (to check are we in git hub or not)
git remote add "GIT HUB URL" (getting into the github)
ssh-keygen -t rsa (to create a ssh public private key)
git checkout -b <NEW BRANCH NAME> (to create a new branch from main branch)
git checkout main (jumps to the main branch)
git cherry-pick <NUMBER ID OF COMMIT> (to merge the branch from the current branch)
git log --oneline 
git rebase <BRANCH NAME>
git merge <BRANCH NAME>
git remote show origin (display the path to push into repo)
